using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReadMe : MonoBehaviour
{
    /* 보강
     * 1. Main Scene에서 버튼이 클릭되지 않는 이유
     *    - Canvas는 있으나 각 Scene의 EventSystem이 없다
     * 
     * 
     * 
     */
    
}
